function sumSomething(x, y) {
    return x + y;
}

function multiplySomething(x, y) {
    return x * y;
}

function divideSomething(x, y) {
    return x / y;
}

module.exports.sumSomething = sumSomething;
module.exports.mulSomething = multiplySomething;
module.exports.divSomething = divideSomething;